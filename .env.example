# Postgres connection string. A Neon URL in practice; a local Postgres for tests.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/givework

# Optional: HTTP port for the server (default 3000).
# PORT=3000
